#/bin/sh
echo `readlink -f "$1"`
osascript \
    -e 'on run{a}' \
    -e 'tell application "Alfred 5" to search a' \
    -e end \
    "`readlink -f "$@"`"


# -e 'tell application "System Events" to keystroke (ASCII character 09)' \